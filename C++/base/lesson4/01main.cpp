#include <stdio.h>
#include <stdlib.h>
#include <iostream>

int main(void) {

    // -1 ? system("calc") : system("mspaint");

    int a = 3;

    // 在C语言中不合法
    
    // (a = 3) = 4; 

    std::cout << a << std::endl;

    return 0;
}